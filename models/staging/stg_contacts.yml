version: 2

models:
  - name: stg_contacts
    description: One row per contact.
    columns:
      - name: contact_id
        description: Primary key.
        tests:
          - unique
          - not_null
